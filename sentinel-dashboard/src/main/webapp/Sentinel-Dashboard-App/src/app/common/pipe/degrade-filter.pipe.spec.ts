import { DegradeFilterPipe } from './degrade-filter.pipe';

describe('DegradeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DegradeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
