import { AppFilterPipe } from './app-filter.pipe';

describe('AppFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AppFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
