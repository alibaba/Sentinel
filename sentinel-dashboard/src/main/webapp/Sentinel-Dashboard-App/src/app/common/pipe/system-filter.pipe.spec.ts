import { SystemFilterPipe } from './system-filter.pipe';

describe('SystemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SystemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
