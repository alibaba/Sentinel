import { FlowFilterPipe } from './flow-filter.pipe';

describe('FlowFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FlowFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
