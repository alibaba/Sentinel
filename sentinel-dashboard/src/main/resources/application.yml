rule:
  configserver:
    appollo:
      env: ${configserver.env:}
      appId: ${configserver.appId:}
      token: ${configserver.token:}
      username: ${configserver.username:apollo}
      cluster: ${configserver.cluster:}
      namespace: ${configserver.namespace:}
      server-addr: ${configserver.address:}
    nacos:
      server-addr: ${configserver.address:}
      namespace: ${configserver.namespace:}
    zookeeper:
      server-addr: ${configserver.address:}

spring:
  influx:
#    url: "http://127.0.0.1:8086"
    database: sentinel_db
