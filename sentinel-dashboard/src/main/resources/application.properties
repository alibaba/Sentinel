#spring settings
spring.http.encoding.force=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true

#cookie name setting
server.servlet.session.cookie.name=sentinel_dashboard_cookie

#logging settings
logging.level.org.springframework.web=INFO
logging.file=${user.home}/logs/csp/sentinel-dashboard.log
logging.pattern.file= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
#logging.pattern.console= %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

#auth settings
auth.filter.exclude-urls=/,/auth/login,/auth/logout,/registry/machine,/version
auth.filter.exclude-url-suffixes=htm,html,js,css,map,ico,ttf,woff,png
# If auth.enabled=false, Sentinel console disable login
auth.username=sentinel
auth.password=sentinel

# Inject the dashboard version. It's required to enable
# filtering in pom.xml for this resource file.
sentinel.dashboard.version=@project.version@

## rule repository type, {memory, nacos}
rule.repository.type=memory
## nacos plugin configure
#nacos.server.ip=192.168.1.51
#nacos.server.port=8848
#nacos.server.namespace=
## group name, default is "SENTINEL_GROUP"
#nacos.server.group-id=

## flow rule date id suffix, default is "-flow-rules"
#nacos.server.flowSuffix=   

## degrade rule date id suffix, default is "-degrade-rules"
#nacos.server.degradeSuffix=

## params rule date id suffix, default is "-params-rules"
#nacos.server.paramSuffix=

## system rule date id suffix, default is "-system-rules"
#nacos.server.systemSuffix=

## authority rule date id suffix, default is "-authority-rules"
#nacos.server.authoritySuffix=

## gateway rule date id suffix, default is "-gateway-rules"
#nacos.server.gatewaySuffix=

## gateway-api rule date id suffix, default is "-gateway-api-rules"
#nacos.server.gatewayApiSuffix=

